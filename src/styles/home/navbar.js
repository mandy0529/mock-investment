import styled from 'styled-components';

const NavbarWrapper = styled.div`
  padding: 2rem;
  background: var(--color-middle-black);
  a {
    color: white;
    font-size: 1.5rem;
    font-weight: bold;
    margin: 1rem 3rem;
  }
`;

export {NavbarWrapper};
